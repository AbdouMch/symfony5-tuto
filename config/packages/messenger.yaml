framework:
  messenger:
    buses:
      command_bus:
        middleware:
          - validation
    # reset services after consuming messages
    reset_on_message: true

    # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
    failure_transport: failed

    transports:
      # https://symfony.com/doc/current/messenger.html#transport-configuration
      app.questions_download: '%env(MESSENGER_TRANSPORT_DSN)%'
      app.token_used: 'doctrine://default?queue_name=token_used'
      failed: 'doctrine://default?queue_name=failed'

    routing:
      # Route your messages to the transports
      'App\Messenger\Message\QuestionExport': app.questions_download
      'App\Messenger\Message\TokenUsed': app.token_used
